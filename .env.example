# Separate from config.json api_key. Use a long, random management password.
WB2A_ADMIN_PASSWORD=
# Enable when accessed through an HTTPS reverse proxy preserving the Host header.
WB2A_ADMIN_SECURE_COOKIE=false
